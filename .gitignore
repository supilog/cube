# OS
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~
.fleet/

# Environment (root level)
.env
.env.local
.env.*.local
.env.backup

# PHP / Composer (root level)
composer.phar
.phpunit.cache
.phpunit.result.cache
vendor/

# Node (root level; src/ has its own)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build / cache (root level)
*.log
.cache/

# Docker (local overrides; keep docker-compose.yml tracked)
docker-compose.override.yml
.docker/

# Misc
*.bak
*.tmp
*.temp
Homestead.json
Homestead.yaml
auth.json
